# 星课 StarClass 使用手册（Web 工作台）

## 1. 系统组成

| 部分 | 地址 | 说明 |
|------|------|------|
| 后端 API | `http://localhost:3001/api` | Node.js + Express + SQLite，本地/自有服务器运行 |
| Web 管理工作台 | `http://localhost:3001` | Vue3 单入口（生产构建版），管理员日常操作全在这里 |

**Web 端导航（7 大模块）**：数据看板 / 教学运营（课程、排期、签到、请假）/ 学员档案（成员、积分）/ 销售工作台（销售、增长）/ 家校沟通（通讯录、通知、反馈）/ 团队管理（员工、教练课时）/ 系统设置。旧页面地址（如 `/orders`、`/schedule`）自动跳转到对应模块，无需记忆新地址。

界面支持 🌙 **深色模式**：顶栏右侧按钮可在「跟随系统 / 浅色 / 深色」之间切换，偏好自动记忆，刷新与重开均保持。

## 2. 启动步骤

```bash
# 首次：初始化数据库
node backend/db/init.js

# 后端
cd backend && node server.js
```

**单入口方式（推荐）**：只启动后端（`node backend/server.js`），访问 `http://localhost:3001` 即为管理工作台（生产构建版），无需单独启动前端服务。管理端代码更新后需重新执行 `cd web-admin && npm run build`。

**开发模式**（改前端代码时）：新开终端 `cd web-admin && npm run dev`，Vite 热更新。

**一键就绪**：直接运行 `./start-all.sh`，会自动安装依赖（如缺失）、初始化并填充数据库、构建管理端单入口、随后启动后端与开发版管理端。全新机器更简单：`bash deploy.sh`（见 README）。

## 3. 账号与权限

**超级管理员**（测试全部功能首选）：手机号 `13800000001`，密码 `123456`。

| 身份 | 手机号（示例） | 密码 | 登录方式 | 权限 |
|------|------|------|----------|------|
| 管理员 | `13800000001` | `123456` | 手机号 + 身份选择 + 密码 | Web 工作台全部功能：成员、排课、签到、请假审批、通知、反馈、销售、薪资、设置 |
| 教练 | `13800000011` 王教练 | `123456` | 手机号 + 身份选择 + 密码 | 教练档案与课时数据（可选付费小程序教练端可自助查看课表 / 点名 / 点评） |
| 家长 | `13900000001` 小明爸爸 | 无需密码 | 手机号 + 身份选择 | 家长与绑定成员档案、通知与反馈接收（可选付费小程序家长端自助查看） |

> Web 工作台仅管理员可进入；家长 / 教练账号登录 Web 会被拦截并提示。
> 管理员在「系统设置 → 账号安全」可修改自己的登录密码；「团队管理 → 员工」新增教练会自动创建教练登录账号（默认密码 `123456`），停用教练后其账号同步停用、无法登录。
> 默认密码仅供测试，正式上线前请务必在「账号安全」中修改。
> 家长端 / 教练端 / 管理端**微信小程序为可选付费扩展**，不在本仓库内；后端已预留微信登录、订阅消息与支付能力，购买授权后即可对接部署。获取方式见 README「可选付费扩展」一节。

## 4. 核心工作流

### 4.1 发布通知 → 家长接收

1. Web「家校沟通 → 通知」编辑标题、内容、优先级，可面向全部或指定班级定向下发
2. 通知同时写入家长站内消息（购买小程序扩展后，家长在小程序通知中心即时收到，并可通过微信订阅消息推送）
3. 可发布面向特定小组的通知（活动页按小组展示）

### 4.2 排课 → 报名 → 签到

1. 「教学运营 → 排期」新建排期：选活动 / 教师 / 场地 / 时间，可设重复规则（每天、每周选星期、按 N 天间隔 + 结束日期），自动冲突检测
2. 活动报名：标记为公开活动的排期开放报名（Web 端录入报名，或小程序家长端自助报名），人数实时同步
3. 当日「教学运营 → 签到」点名（默认按到场），结束后未签到成员每日 23:30 自动标记缺席
4. 活动开始前 2 小时自动向已报名家长推送「训练即将开始」提醒

### 4.3 销售 → 会员卡 → 续费提醒

1. 「销售工作台 → 销售」新建销售单：选会员、产品、签单人、收款日期，确认收款后自动激活会员卡
2. 管理员可暂停会员卡（需填原因），恢复时按暂停天数自动顺延有效期，档案内显示「已暂停」状态
3. 到期前 30 / 15 / 7 / 3 / 1 天（设置页可配置）自动向家长推送续费提醒

### 4.4 产品价格调整

「销售工作台 → 产品服务」可新增、停用、调整会员卡产品（月卡/季卡/年卡/体验卡等）及周边商品价格；「系统设置 → 机构信息」可设置客服电话与机构称呼，保存后全站即时生效，无需重启。

### 4.5 请假审批

1. 家长提交请假申请（小程序请假页，或由管理员在 Web「教学运营 → 请假」代为录入）
2. Web「请假管理」按状态筛选，批准或驳回（可填原因）
3. 批准后自动记录该成员请假考勤（不再计缺席），家长立即收到「请假已批准 / 未通过」通知
4. 可一键安排补课

### 4.6 家长沟通

1. 「家校沟通 → 通讯录」查看家长通讯录（姓名、手机号、绑定成员）
2. 可给单个家长发送专属通知（标题 + 内容）
3. 「家校沟通 → 反馈」查看全部反馈，可标记已处理或重新打开；回复会同步给家长

### 4.7 教练课时与薪资

1. 「团队管理 → 教练课时」查看各教练签到课时明细与统计
2. 「团队管理 → 薪资」按课时 / 人头 / 混合计费规则结算，课时费规则在「系统设置」中可视化配置

## 5. 数据导出（Web 端）

所有导出统一为「弹窗二次确认」：点击导出 → 在弹窗中选择时间范围（可留空导出全部）→ 点击「确认导出」后才生成文件，避免误点。

- 成员管理：Excel（xlsx）含联系方式、卡种、剩余课时、到期日、累计消费、购买次数
- 排期管理：Excel（xlsx）含日期、活动、教师、场地、时间、人数
- 销售管理：Excel（xlsx）含单号、会员、项目、金额、签单人、状态
- 看板：今日/本周/本月/本年收入、签单排名、1v1 销售、到场率、续期预警

成员管理与销售管理的表格均支持「字段设置」（显示/隐藏列，保存后持久化）；成员管理还支持按当前项目（卡种）与状态组合筛选。

成员管理字段说明：列表与详情含序号、姓名、联系方式、出生日期、年龄、性别、当前项目、训练级别、开始日期、到期日期、购买次数、新购日期、累计消费、就读学校、状态。购买次数与累计消费由销售单自动汇总（仅统计已收款订单），与销售管理数据实时一致。

## 6. 数据备份与迁移

- **自动备份**：每日备份落在 `backend/backups/`；「系统设置」页支持一键下载当前库备份
- **手动备份**：`sqlite3 backend/db/data.db ".backup 'backup-$(date +%F).db'"`
- **迁移**：拷走 `backend/db/data.db` + `backend/uploads/` 两处，新机重跑 `bash deploy.sh` 后放回数据文件即可
- **重置演示数据**：删除 `backend/db/data.db*` 后重新执行 `node backend/db/init.js`（可选 `node backend/db/seed.js` 填充示例数据）

## 7. 常见问题

- **登录提示频繁**：登录限流 100 次/15 分钟、全局限流 200 次/分钟，重启后端即复位
- **忘记管理员密码**：示例库可直接删库重来；正式数据请勿删库，在「系统设置 → 账号安全」修改
- **端口 3001 被占用**：先 `bash stop-all.sh`；仍占用则 `export PORT=3002 && bash start-all.sh`
- **深色模式显示异常**：顶栏切换按钮可强制指定浅色/深色；若某页面深浅色下仍有违和色块，请在 Issues 反馈（附页面地址与截图）
- **微信登录 / 订阅消息失败**：需配置 `WX_APPID` / `WX_SECRET`，仅在使用付费小程序扩展时需要；纯 Web 部署用手机号登录即可
- 更多排障见 `docs/常见问题FAQ.md`

## 8. 自动验证

项目根目录提供端到端冒烟测试，覆盖登录、看板、报名、请假审批、考勤、通知、权限隔离等核心业务闭环：

```bash
node smoke-test.mjs   # 或 npm run smoke
npm run verify        # 全量验收套件（需服务运行中，见 TEST-GUIDE.md）
```

全部通过退出码为 0，任何一步失败会打印失败明细并以非 0 退出码结束。
