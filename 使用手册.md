# 教务系统使用手册

## 1. 系统组成

| 部分 | 地址 | 说明 |
|------|------|------|
| 后端 API | `http://localhost:3001` | Node.js + Express + SQLite，免费本地运行 |
| Web 管理端 | `http://localhost:3000`（端口被占用时用 `http://localhost:3001` 单入口） | Vue3 管理后台，仅管理员可用 |
| 微信小程序 | 微信开发者工具导入 `miniprogram/` | 家长 / 教练 / 管理员共用 |

**Web 端导航（7 大模块）**：数据看板 / 教学运营（课程、排期、签到、请假）/ 学员档案（成员、积分）/ 销售工作台（销售、增长）/ 家校沟通（通讯录、通知、反馈）/ 团队管理（员工、教练课时）/ 系统设置。旧页面地址（如 `/orders`、`/schedule`）自动跳转到对应模块，无需记忆新地址。

## 2. 启动步骤

```bash
# 首次：初始化数据库
node backend/db/init.js

# 后端
cd backend && node server.js

# Web 管理端（新开终端）
cd web-admin && npm run dev
```

小程序在微信开发者工具中导入 `miniprogram/` 目录即可。API 地址在 `miniprogram/config.js` 中（`API_BASE_URL`），默认 `http://localhost:3001/api`；开发者工具下默认连本机，真机/远程调试自动回退内网穿透隧道，体验版/正式版自动使用线上域名。

**单入口方式**：如只启动后端（`node backend/server.js`），访问 `http://localhost:3001` 即为管理端（生产构建版），无需单独启动 Vite；管理端代码更新后需重新执行 `cd web-admin && npm run build`。

**一键就绪**：直接运行 `./start-all.sh`，会自动安装依赖（如缺失）、初始化并填充数据库、构建管理端单入口、随后启动后端与开发版管理端。

## 3. 账号与权限

**超级管理员**（测试全部功能首选）：手机号 `13800000001`，密码 `123456`。

| 身份 | 手机号（示例） | 密码 | 登录方式 | 权限 |
|------|------|------|----------|------|
| 管理员 | `13800000001` | `123456` | 手机号 + 身份选择 + 密码 | Web 全部功能、小程序管理端工作台（销售 / 成员 / 点名 / 请假 / 积分 / 增长 / 活动 / 班级 / 通知 / 课时等入口） |
| 教练 | `13800000011` 王教练 | `123456` | 手机号 + 身份选择 + 密码 | 小程序查看课表、活动、点名签到、请假审批、课后点评、本人课时统计 |
| 家长 | `13900000001` 小明爸爸 | 无需密码 | 手机号 + 身份选择 | 小程序查看孩子训练、报名、购买、接收通知 |

> Web 管理端仅管理员可进入；家长/教练登录 Web 会被拦截并提示。
> 管理员在 Web「系统设置 → 账号安全」可修改自己的登录密码；Web「员工管理」新增教练会自动创建教练登录账号（默认密码 `123456`），停用教练后其账号同步停用、无法登录。
> 管理员小程序端入口：底部「工作台」提供 销售管理（录入/改价/取消订单、签单人选择）、成员管理（筛选/详情/暂停恢复卡）、排课管理（活动页新建/修改/取消）、今日点名（逐课考勤，教练也可用）、请假审批、积分管理、增长中心（线索跟进/续费预警）、班级管理（项目启停）、发布通知（支持按班级定向）；「我的 → 设置」提供 员工管理（教练账号/权限/重置密码）、系统设置（机构信息/客服）、意见反馈、身份切换；首页展示今日训练（含已签到统计），数据看板由工作台「本月收费」进入。完整成员资料编辑、报表导出仍以 Web 管理端为准。
> 默认密码仅供测试，正式上线前请务必在「账号安全」中修改。
> 微信一键登录需在启动后端前配置 `WX_APPID` / `WX_SECRET` 环境变量，未配置时自动回退手机号登录。

**真机调试与上线**：小程序 API 地址在 `miniprogram/config.js` 的 `API_BASE_URL` 配置。开发者工具自动使用本机 `http://localhost:3001/api`；体验版/正式版自动切换到正式域名（上线前把 `config.js` 中的正式地址替换为你的服务器域名，并在微信公众平台「开发管理 → 开发设置 → 服务器域名」添加该域名）。

## 4. 核心工作流

### 4.1 发布通知 → 家长接收

1. 管理员在 Web「成员管理」或小程序「我的 → 发布通知」编辑标题、内容、优先级
2. 家长小程序「首页」顶部通知窗口展示最新 2 条，点击进入详情（标题 / 标签 / 时间 / 正文单卡）
3. 可发布面向特定小组的通知（活动页按小组展示）

### 4.2 排课 → 家长报名 → 签到

1. Web「排期管理」新建排期：选活动 / 教师 / 场地 / 时间，可设重复规则（每天、每周选星期、按 N 天间隔 + 结束日期），自动冲突检测
2. 小程序「活动」页显示课表，活动开始前 24 小时内开放报名；家长报名/取消报名实时同步人数
3. 当日 Web「签到管理」点名（默认按到场），结束后未签到成员每日 23:30 自动标记缺席
4. 活动开始前 2 小时自动向已报名家长推送「训练即将开始」提醒

### 4.3 销售 → 会员卡 → 续费提醒

1. Web「销售管理」新建销售单：选会员、产品、签单人、收款日期，确认收款后自动激活会员卡
2. 会员卡信息同步到小程序「会员」页（剩余课时、有效期、购买时间）
3. 管理员可暂停会员卡（需填原因），恢复时按暂停天数自动顺延有效期；小程序显示「已暂停」状态
4. 到期前 30 / 15 / 7 / 3 / 1 天（设置页可配置）自动向家长推送续费提醒

### 4.4 产品价格调整

Web「销售增长 → 产品服务」可新增、停用、调整会员卡产品（月卡/季卡/年卡/体验卡等）及球服等周边商品价格，「系统设置 → 机构信息」可设置客服电话，保存后小程序「发现」「会员」「联系客服」即时同步，无需发版。

### 4.5 请假审批

1. 家长在小程序「首页 → 请假」或「请假」页，从已报名的未来活动中选择活动、填写原因并提交
2. Web「请假管理」按状态筛选，批准或驳回（可填原因）
3. 批准后自动记录该成员请假考勤（不再计缺席），家长立即收到「请假已批准/未通过」通知
4. 家长可在小程序「请假」页查看申请状态与审批备注

### 4.6 家长沟通

1. Web「家长沟通」查看家长通讯录（姓名、手机号、绑定成员）
2. 可给单个家长发送专属通知（标题 + 内容）
3. 家长在小程序「通知中心」即时收到并查看详情

### 4.7 意见反馈

1. 家长在小程序「我的 → 意见反馈」填写内容（可留联系方式）并提交
2. Web「意见反馈」查看全部反馈，可标记已处理或重新打开
3. 家长在小程序查看反馈处理状态

## 5. 数据导出（仅 Web 端）

所有导出统一为「弹窗二次确认」：点击导出 → 在弹窗中选择时间范围（可留空导出全部）→ 点击「确认导出」后才生成文件，避免误点。

- 成员管理：「导出」按钮，Excel（xlsx）含联系方式、卡种、剩余课时、到期日、累计消费、购买次数
- 排期管理：「导出」按钮，Excel（xlsx）含日期、活动、教师、场地、时间、人数
- 销售管理：「导出」按钮，Excel（xlsx）含单号、会员、项目、金额、签单人、状态
- 看板：今日/本周/本月/本年收入、签单排名、1v1 销售、到场率、续期预警

成员管理与销售管理的表格均支持「字段设置」（显示/隐藏列，保存后持久化）；成员管理还支持按当前项目（卡种）与状态组合筛选。

成员管理字段说明：列表与详情含序号、姓名、联系方式、出生日期、年龄、性别、当前项目、训练级别（新增，可在添加/编辑成员时填写）、开始日期、到期日期、购买次数、新购日期、累计消费、就读学校、状态。购买次数与累计消费由销售单自动汇总（仅统计已收款订单），与销售管理数据实时一致；「字段设置」可自行显示/隐藏列。

## 6. 常见问题

- **小程序连不上后端**：确认后端已启动在 3001 端口；真机/远程调试无需改代码——运行 `bash tools/tunnel-status.sh` 启动内网穿透隧道后会自动同步地址到 `miniprogram/local-config.js`；正式上线需在微信公众平台配置 request 合法域名。更多排障见 `docs/常见问题FAQ.md`
- **登录提示频繁**：登录限流 100 次/15 分钟、全局限流 200 次/分钟，重启后端即复位
- **广告不显示**：在 `miniprogram/config.js` 填入真实微信流量主广告位 ID
- **微信登录失败**：检查是否配置 `WX_APPID` / `WX_SECRET`，未配置请用手机号登录
- **数据重置**：删除 `backend/db/data.db` 后重新执行 `node backend/db/init.js`（可选 `node backend/db/seed.js` 填充示例数据）

## 7. 自动验证

项目根目录提供端到端冒烟测试，覆盖登录、看板、报名、请假审批、考勤、通知、权限隔离等核心业务闭环：

```bash
node smoke-test.mjs   # 或 npm run smoke
```

全部通过退出码为 0，任何一步失败会打印失败明细并以非 0 退出码结束。
