# 部署上线说明

本文档说明如何把**星课管理工作台**从本地开发走向真实上线（后端 API + Web 管理端，同端口单入口）。

> 三端微信小程序为可选付费扩展，不在本仓库内，其上传发布流程随扩展部署授权一并提供。
> 纯 Web 部署无需任何微信配置：手机号 + 密码登录全功能可用。

## 1. 快速上线（推荐 · Docker 一键）

```bash
git clone https://github.com/Mihooni/edu-admin.git
cd edu-admin
./deploy/deploy.sh --host app.yourdomain.com    # 自动 HTTPS（Caddy 边车）
# 内网 / 无域名环境：./deploy/deploy.sh --no-https
```

单容器交付（API + 管理工作台同端口），SQLite 落在数据卷中跨升级保留。
也可在 GitHub Actions 里点 **Deploy → Run workflow** 全自动部署，详见 [`deploy/README.md`](deploy/README.md)。

部署完成后访问 `https://你的域名/` 即为管理工作台；健康检查 `https://你的域名/api/health`。

## 2. 手动上线（免 Docker）

### 2.1 环境要求
- Node.js ≥ 18（推荐 20/22）
- 公网服务器 + 已备案域名 + HTTPS 证书（小程序扩展对接时需要；纯内网使用可省域名）

### 2.2 构建与启动

```bash
# 1. 安装依赖
bash deploy.sh          # 自动装依赖 + 初始化库 + 构建管理端 + 启动

# 或分步：
cd web-admin && npm ci && npm run build && cd ..
cd backend && npm ci

# 2. 生产环境变量（JWT_SECRET 必填，未设置会拒绝启动）
export NODE_ENV=production
export JWT_SECRET=$(openssl rand -hex 32)
# 3. 启动（建议 pm2 守护）
npm install -g pm2
cd backend && pm2 start server.js --name edu-admin && pm2 save && pm2 startup
```

### 2.3 反向代理与 HTTPS
- Caddy（推荐，自动证书）：参考 `deploy/Caddyfile`
- Nginx：`location / { proxy_pass http://127.0.0.1:3001; }` + 证书，SPA 回退已由后端内置，无需额外配置

## 3. 数据与备份

- 数据文件：`backend/db/data.db`（SQLite 单文件，含全部业务数据）
- 每日自动备份在 `backend/backups/`；「系统设置」页支持一键下载备份
- 手动备份：`sqlite3 backend/db/data.db ".backup 'backup-$(date +%F).db'"`
- 迁移：拷走 `backend/db/data.db` + `backend/uploads/` 两处，新机重跑部署后放回即可
- 重置数据：删除 `data.db*` 后重新 `node backend/db/init.js`（可选 `seed.js` 填充示例）

## 4. 安全注意事项

- 上线后把 `NODE_ENV` 设为 `production`，并把 CORS 来源改为你的正式域名
- 生产环境强制 `JWT_SECRET`（未设置拒绝启动）；请立即修改示例管理员密码
- 登录接口限流已启用（100 次/15 分钟），全局限流 200 次/分钟
- 管理接口仅管理员可访问；家长 / 教练数据按 openid 隔离
- 定期备份数据库，重要操作前先备份

## 5. 上线自检清单

- [ ] `https://域名/api/health` 返回正常
- [ ] `https://域名/` 可打开管理工作台（深色 / 浅色显示正常）
- [ ] 管理员手机号登录可用，家长 / 教练账号登录被拦截
- [ ] 已修改默认管理员密码（系统设置 → 账号安全）
- [ ] CORS / `NODE_ENV=production` 已配置
- [ ] 冒烟测试通过：`API_BASE=https://域名/api node smoke-test.mjs`
- [ ] 数据库已备份，且定时备份可访问（`backend/backups/`）

## 6. 常见问题

### 管理端打不开 / 白屏
1. 确认后端进程存活：`curl http://127.0.0.1:3001/api/health`
2. 确认已构建：`web-admin/dist/` 存在且非空（`cd web-admin && npm run build`）
3. 反向代理是否把 `/` 转发到了 3001 端口

### 端口 3001 被占用
`bash stop-all.sh` 停旧进程；仍需换端口：`export PORT=3002` 后启动，并同步反向代理目标端口。

### 升级版本
`git pull && bash deploy.sh`（不会动你的数据）；Docker 部署重新跑 `deploy/deploy.sh` 即可，数据卷保留。

### 想购买三端小程序
在 [Issues](https://github.com/Mihooni/edu-admin/issues) 留言联系获取部署授权；后端已预留微信登录、支付与订阅消息端点，购买后按扩展仓库文档配置即可。
