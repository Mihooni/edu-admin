# 部署上线说明

本文档说明如何从本地开发走向真实上线（微信小程序 + 管理端）。

## 1. 小程序上线

### 1.1 准备
- 在微信公众平台注册小程序，获取 `AppID` 与 `AppSecret`
- 后端服务器需要公网可访问（或内网穿透），并配置 HTTPS 域名

### 1.2 配置
1. 后端环境变量：
   ```bash
   export WX_APPID=你的AppID
   export WX_SECRET=你的AppSecret
   export JWT_SECRET=$(openssl rand -hex 32)   # 必填：生产环境未设置会拒绝启动
   ```
2. 小程序 `miniprogram/config.js` 的 `API_BASE_URL` 改为线上地址，例如 `https://api.yourdomain.com/api`（开发者工具自动连本机 `http://localhost:3001/api`，体验版/正式版自动用正式域名；只需在 `config.js` 填正式地址即可）
3. 微信公众平台「开发管理 → 服务器域名」添加合法域名：
   - request 合法域名：`https://api.yourdomain.com`
4. 登录体验：开发版/体验版使用手机号 + 身份登录（管理员/教练需密码，默认 `123456`，上线后请在「系统设置 → 账号安全」修改）；正式版可引导用户使用微信一键登录（需后端已配置 AppID/Secret）

### 1.3 上传与发布
1. 微信开发者工具 →「上传」，填写版本号与备注
2. 公众平台提交审核 → 审核通过后发布

### 1.4 广告位
- 公众平台开通“流量主”后，把真实广告位 ID 填入 `miniprogram/config.js` 的 `AD_UNIT_ID`

## 2. 管理端上线

### 2.1 构建
```bash
cd web-admin && npm run build
```
构建产物在 `web-admin/dist/`，后端 Express 已内置静态服务与 SPA 回退：
- 访问 `https://api.yourdomain.com/` 即管理端
- 仅管理员可登录；数据导出仅在 web 端

### 2.2 进程守护（可选）
```bash
npm install -g pm2
cd backend && pm2 start server.js --name edu-admin
pm2 save && pm2 startup
```

## 3. 数据与备份

- 数据文件：`backend/db/data.db`（SQLite 单文件，含全部业务数据）
- 备份：直接复制该文件即可（建议先停服或用 `sqlite3 .backup`）
  ```bash
  sqlite3 backend/db/data.db ".backup 'backup-$(date +%F).db'"
  ```
- 重置数据：删除 `data.db*` 后重新 `node backend/db/init.js`（可选 `seed.js` 填充示例）

## 4. 安全注意事项

- 上线后建议将 `NODE_ENV` 设为 `production`，并把 CORS 来源改为你的正式域名
- 登录接口限流已启用（100 次/15 分钟），全局限流 200 次/分钟
- 管理接口仅管理员可访问；家长/教练数据按 openid 隔离
- 定期备份数据库，重要操作前先备份

## 5. 上线自检清单

- [ ] 后端 `WX_APPID` / `WX_SECRET` 已配置，微信一键登录可用
- [ ] 小程序 `config.js` 的 `API_BASE_URL` 指向线上 HTTPS 域名
- [ ] 公众平台合法域名已配置
- [ ] `web-admin` 已构建，`https://域名/` 可打开管理端
- [ ] 管理员手机号登录可用，家长/教练被拦截
- [ ] 冒烟测试通过：`API_BASE=https://域名/api node smoke-test.mjs`
- [ ] 数据库已备份

## 6. 常见问题：体验版/正式版无法登录

### 原因
小程序「开发版」连本机 `http://localhost:3001/api`；但**体验版（trial）与正式版（release）会强制使用 `miniprogram/config.js` 中的正式域名**（默认是占位地址 `https://api.your-domain.com/api`）。该域名不存在时，登录等所有请求都会失败，页面提示「后端未部署」或「网络异常」。

### 解决步骤
1. 把后端部署到公网服务器（任意云服务器 + Nginx + HTTPS 证书），确保 `curl https://你的域名/api/health` 返回正常
2. 修改 `miniprogram/config.js`，把 `API_BASE_URL` 的正式域名替换为你的真实域名（保留 `develop` 分支，开发版仍连本机）
3. 微信公众平台 → 开发管理 → 开发设置 → 服务器域名 → 「request 合法域名」添加 `https://你的域名`（域名需已 ICP 备案）
4. 线上数据库初始化管理员账号：`node backend/db/seed.js`（默认管理员 `13800000001` / `123456`），上线后及时改密
5. 重新「上传」并设为体验版，再登录测试

### 想先在自己手机上体验（不部署服务器）
- 不要上传体验版，改用开发者工具「真机调试」：手机与电脑连**同一 Wi-Fi**（路由器勿开「AP 隔离」）
- 在 `miniprogram/config.js` 填 `LAN_HOST = '电脑局域网IP:3001'`（查看 IP：终端运行 `ipconfig getifaddr en0`）
- 开发者工具勾选「不校验合法域名…」，选择真机调试即可在手机登录
- **iOS 必须允许微信访问本地网络**：手机「设置 → 微信 → 本地网络」打开，否则局域网请求全部失败
- 换网络或电脑 IP 变化后，需同步更新 `LAN_HOST`（可在电脑上先验证：`curl http://电脑IP:3001/api/health`）
- 注意：局域网方案只用于开发调试，家长/教练使用必须走第 1-5 步的正式部署

### 不在同一网络（远程测试）
电脑与手机不在同一 Wi-Fi 时，用内网穿透把本机后端暴露成公网 HTTPS 地址：
1. 安装 cloudflared（macOS：`brew install cloudflared`），然后一条命令启动隧道：
   ```bash
   bash tools/tunnel-status.sh
   ```
   脚本会自动：检查后端 → 启动 `cloudflared` 隧道 → 读取公网地址 → 同步写入 `miniprogram/local-config.js`（无需改 `config.js`）
2. 微信开发者工具重新编译，勾选「不校验合法域名…」，点「真机调试」扫码
3. 手机使用任意网络（4G/5G/其他 Wi-Fi）即可访问电脑上的后端进行登录测试

要点：
- 隧道地址形如 `https://xxx.trycloudflare.com`，**每次隧道重启地址会变化**，重启后重跑 `bash tools/tunnel-status.sh` 会自动更新配置
- 远程调试只用于开发阶段；体验版/正式版仍必须走「服务器 + 已备案域名 + request 合法域名」的正式部署
